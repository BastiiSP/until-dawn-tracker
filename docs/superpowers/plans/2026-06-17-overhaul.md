# Until Dawn Tracker — Major Overhaul Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Kompletter Design- und UX-Overhaul: authentisches Until Dawn Feeling, Karten-Navigation (Card-by-Card), Run-Zeitstrahl, deutsche Lokalisierung, Undo-Funktion.

**Architecture:** Die Listenansicht (`DecisionList` + `DecisionCard` + `TimedDecision`) wird vollständig durch ein swipebares Kartensystem (`CardNavigator` + `ButterflyCard`) ersetzt. Timed Mode entfällt. Run-Vergleich wird direkt in jede Karte integriert + eigene Timeline-Seite. Alle UI-Texte auf Deutsch.

**Tech Stack:** Next.js 16 App Router, Tailwind CSS v4, Supabase, Cinzel Decorative (neue Font), native Touch Events für Swipe.

---

## File Map

```
MODIFY:
  src/app/globals.css                           ← Farbpalette (Eisblau, Gold), neue Keyframes
  src/app/layout.tsx                            ← Cinzel Decorative Font hinzufügen
  src/app/auth/login/page.tsx                   ← Sanduhr-Redesign, deutsche Texte
  src/app/runs/page.tsx                         ← deutsche Texte
  src/app/runs/new/page.tsx                     ← deutsche Texte
  src/app/runs/[id]/page.tsx                    ← CardNavigator statt DecisionList, alle Runs-Daten fetchen
  src/app/runs/[id]/compare/page.tsx            ← RunTimeline statt ComparisonView
  src/components/characters/CharacterCard.tsx   ← Eisblau für "Unbekannt", deutsche Labels
  src/components/effects/MountainSilhouette.tsx ← dramatischeres SVG
  src/components/effects/LightningEffect.tsx    ← SVG-Blitze mit Wandereffekt
  src/lib/data/butterfly-effects.ts             ← nameDe, descriptionDe, optionsDe, sceneImage hinzufügen

CREATE:
  src/components/decisions/ButterflyCard.tsx    ← Einzelkarte: Bild, Szene, Optionen, Badges
  src/components/decisions/CardNavigator.tsx    ← Swipe-Navigation, Progress-Indicator
  src/components/comparison/RunTimeline.tsx     ← Zeitstrahl aller Runs
  public/scenes/                                ← 22 Szenenbilder (download during execution)

DELETE:
  src/components/decisions/DecisionCard.tsx
  src/components/decisions/DecisionList.tsx
  src/components/decisions/TimedDecision.tsx
  src/components/decisions/TimedModeButton.tsx
  src/components/comparison/ComparisonView.tsx
```

---

## Task 1: Charakterstatus "Unbekannt" → Eisblau

**Files:**
- Modify: `src/components/characters/CharacterCard.tsx`
- Modify: `src/app/globals.css` (neue Farb-Tokens)

- [ ] **Step 1: Eisblau-Farbtokens in globals.css ergänzen**

In `src/app/globals.css` im `@theme`-Block ergänzen (nach `--color-horror-warning`):

```css
  --color-horror-ice: #7db8cc;
  --color-horror-ice-dim: #1a3a4e;
  --color-horror-gold: #c9a84c;
  --color-horror-gold-dim: #3a2a0a;
```

- [ ] **Step 2: CharacterCard Unknown-Styles auf Eisblau ändern**

In `src/components/characters/CharacterCard.tsx` die beiden `unknown`-Einträge ändern:

```typescript
const STATUS_OVERLAY: Record<Status, string> = {
  alive:   'ring-green-600',
  dead:    'ring-red-800',
  unknown: 'ring-[#2a5a7a]',
}

const STATUS_BADGE: Record<Status, string> = {
  alive:   'bg-green-900 text-green-300',
  dead:    'bg-red-950 text-red-400',
  unknown: 'bg-[#0d2030] text-[#7db8cc]',
}
```

- [ ] **Step 3: Verify im Browser**

```bash
npm run dev
```

Öffne `http://localhost:3000/runs` → eine Run öffnen → Charaktere ohne Status sollten jetzt eisblau statt grau sein.

- [ ] **Step 4: Commit**

```bash
git add src/components/characters/CharacterCard.tsx src/app/globals.css
git commit -m "feat: character unknown status → ice blue"
```

---

## Task 2: Design Overhaul

**Files:**
- Modify: `src/app/layout.tsx`
- Modify: `src/app/globals.css`
- Modify: `src/app/auth/login/page.tsx`
- Modify: `src/components/effects/MountainSilhouette.tsx`
- Modify: `src/components/effects/LightningEffect.tsx`

### 2a: Cinzel Decorative Font

- [ ] **Step 1: Cinzel Decorative in layout.tsx laden**

In `src/app/layout.tsx` den Font-Import erweitern:

```typescript
import type { Metadata, Viewport } from 'next'
import { Inter, Cinzel, Cinzel_Decorative } from 'next/font/google'
import './globals.css'
import { SnowEffect } from '@/components/effects/SnowEffect'
import { LightningEffect } from '@/components/effects/LightningEffect'
import { MountainSilhouette } from '@/components/effects/MountainSilhouette'

const inter = Inter({ subsets: ['latin'], display: 'swap' })
const cinzel = Cinzel({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-cinzel-loaded',
  weight: ['400', '700', '900'],
})
const cinzelDecorative = Cinzel_Decorative({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-cinzel-decorative',
  weight: ['400', '700', '900'],
})

export const metadata: Metadata = {
  title: 'Until Dawn Tracker',
  description: 'Track your Until Dawn decisions across multiple runs',
  manifest: '/manifest.webmanifest',
  appleWebApp: {
    capable: true,
    statusBarStyle: 'black-translucent',
    title: 'Until Dawn',
  },
}

export const viewport: Viewport = {
  themeColor: '#050505',
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="de">
      <body className={`${inter.className} ${cinzel.variable} ${cinzelDecorative.variable} bg-horror-bg text-horror-text antialiased`}>
        <SnowEffect />
        <LightningEffect />
        <MountainSilhouette />
        {children}
      </body>
    </html>
  )
}
```

- [ ] **Step 2: Font-Token in globals.css registrieren**

In `src/app/globals.css` im `@theme`-Block nach `--font-cinzel` ergänzen:

```css
  --font-cinzel-decorative: var(--font-cinzel-decorative), Georgia, serif;
```

Und diese Utility-Klasse im `@layer utilities`-Block ergänzen:

```css
  .font-cinzel-decorative {
    font-family: var(--font-cinzel-decorative);
  }

  .text-glow-gold {
    text-shadow: 0 0 15px rgba(201, 168, 76, 0.7), 0 0 30px rgba(201, 168, 76, 0.3);
  }
```

### 2b: Sanduhr-Redesign mit "UNTIL DAWN" Schriftzug

- [ ] **Step 3: Sanduhr-SVG in login/page.tsx ersetzen**

In `src/app/auth/login/page.tsx` das gesamte `<svg>`-Element (Zeilen 31–48) ersetzen mit:

```tsx
          {/* Until Dawn iconic hourglass — black body, gold frame, title integrated */}
          <svg
            viewBox="0 0 80 110"
            className="w-16 h-[88px] mb-5"
            xmlns="http://www.w3.org/2000/svg"
            aria-hidden
          >
            {/* Outer gold frame — top bar */}
            <rect x="4" y="0" width="72" height="6" rx="2" fill="#c9a84c" />
            {/* Outer gold frame — bottom bar */}
            <rect x="4" y="104" width="72" height="6" rx="2" fill="#c9a84c" />

            {/* Hourglass body outline */}
            <path
              d="M8,6 L72,6 L40,52 L72,104 L8,104 L40,52 Z"
              fill="#0a0a0a"
              stroke="#c9a84c"
              strokeWidth="1.5"
            />

            {/* Upper sand (draining) */}
            <path d="M12,9 L68,9 L40,46 Z" fill="#c9a84c" opacity="0.25" />

            {/* Sand stream at neck */}
            <line x1="40" y1="52" x2="40" y2="62" stroke="#c9a84c" strokeWidth="1.5" opacity="0.6" />

            {/* Lower sand (accumulating) */}
            <path d="M22,101 L58,101 L52,72 L28,72 Z" fill="#c9a84c" opacity="0.35" />

            {/* Decorative side pillars */}
            <rect x="4" y="6" width="4" height="98" rx="1" fill="#1a1a1a" stroke="#c9a84c" strokeWidth="0.5" />
            <rect x="72" y="6" width="4" height="98" rx="1" fill="#1a1a1a" stroke="#c9a84c" strokeWidth="0.5" />

            {/* "UNTIL DAWN" text integrated into middle */}
            <text
              x="40"
              y="56"
              textAnchor="middle"
              fill="#c9a84c"
              fontSize="5"
              fontFamily="Georgia, serif"
              letterSpacing="2"
              fontWeight="bold"
            >UNTIL DAWN</text>
          </svg>
```

- [ ] **Step 4: Login-Seite deutschen Text ergänzen (Vorgriff auf Task 6)**

In `src/app/auth/login/page.tsx` alle English-Strings ersetzen:

```typescript
// Zeile mit "Blackwood Mountain — Decision Tracker":
<p className="text-horror-muted text-xs tracking-widest uppercase mt-1">Blackwood Mountain — Entscheidungs-Tracker</p>

// Zeile mit 'Check your email for the magic link.':
<p className="text-horror-text">Prüfe deine E-Mails für den Magic Link.</p>
<p className="text-horror-muted text-sm mt-2">Du kannst diesen Tab schließen.</p>

// Placeholder im Input:
placeholder="deine@email.de"

// Button-Text:
{loading ? 'Sende…' : 'Magic Link senden'}
```

### 2c: Bergsilhouette — dramatischer SVG

- [ ] **Step 5: MountainSilhouette.tsx komplett ersetzen**

Ersetze den kompletten Inhalt von `src/components/effects/MountainSilhouette.tsx`:

```typescript
export function MountainSilhouette() {
  return (
    <div className="pointer-events-none fixed bottom-0 left-0 right-0" aria-hidden>
      <svg
        viewBox="0 0 800 200"
        preserveAspectRatio="xMidYMax slice"
        xmlns="http://www.w3.org/2000/svg"
        className="w-full block"
        style={{ height: '160px' }}
      >
        {/* Nebel-Basis */}
        <defs>
          <linearGradient id="fogGrad" x1="0" y1="0" x2="0" y2="1">
            <stop offset="0%" stopColor="#050505" stopOpacity="0" />
            <stop offset="100%" stopColor="#050505" stopOpacity="1" />
          </linearGradient>
          <linearGradient id="snowGrad" x1="0" y1="0" x2="0" y2="1">
            <stop offset="0%" stopColor="#c8d8e8" stopOpacity="0.9" />
            <stop offset="100%" stopColor="#c8d8e8" stopOpacity="0" />
          </linearGradient>
        </defs>

        {/* Hintergrund-Gebirge (weit entfernt, blasser) */}
        <path
          d="M0,200 L0,140 L60,90 L100,115 L150,65 L200,100 L250,50 L300,88 L350,40 L400,75 L450,35 L500,70 L550,45 L600,80 L650,30 L700,70 L750,55 L800,80 L800,200 Z"
          fill="#0a0c10"
          opacity="0.8"
        />

        {/* Schnee-Highlights auf Hintergrundgipfeln */}
        <path
          d="M150,65 L160,80 L140,80 Z
             M250,50 L262,68 L238,68 Z
             M350,40 L363,60 L337,60 Z
             M450,35 L464,56 L436,56 Z
             M550,45 L562,63 L538,63 Z
             M650,30 L665,52 L635,52 Z"
          fill="url(#snowGrad)"
          opacity="0.6"
        />

        {/* Mittelgrund-Berge (scharf, dramatisch) */}
        <path
          d="M0,200 L0,155 L50,130 L80,145 L120,100 L155,125 L195,80 L225,108 L270,58 L310,90 L345,68 L375,88 L410,48 L445,78 L480,55 L510,80 L545,38 L580,72 L615,58 L650,85 L685,42 L720,75 L760,60 L800,85 L800,200 Z"
          fill="#0d0f12"
        />

        {/* Schnee-Highlights Mittelgrund — Gipfel */}
        <path
          d="M270,58 L285,80 L255,80 Z
             M410,48 L427,73 L393,73 Z
             M545,38 L563,65 L527,65 Z
             M685,42 L702,68 L668,68 Z"
          fill="#c8d8e8"
          opacity="0.7"
        />

        {/* Scharfe Schnee-Kanten auf Mittengipfeln */}
        <path
          d="M270,58 L280,72 L270,70 L260,72 Z
             M410,48 L421,64 L410,62 L399,64 Z
             M545,38 L557,56 L545,54 L533,56 Z
             M685,42 L697,60 L685,58 L673,60 Z"
          fill="#e8f0f8"
          opacity="0.5"
        />

        {/* Vordergrund-Kamm + Tannen */}
        <path
          d="M0,200 L0,175 L30,170 L60,178 L90,165 L120,172 L150,160 L180,170 L210,158 L240,168 L270,155 L300,165 L330,158 L360,170 L390,162 L420,172 L450,160 L480,170 L510,162 L540,172 L570,165 L600,175 L630,168 L660,177 L690,165 L720,174 L750,168 L780,175 L800,170 L800,200 Z"
          fill="#080a0d"
        />

        {/* Tannen-Silhouetten */}
        <path
          d="M0,200 L8,168 L16,200 Z M18,200 L26,162 L34,200 Z M36,200 L46,156 L56,200 Z
             M58,200 L66,165 L74,200 Z M76,200 L85,158 L94,200 Z M96,200 L104,163 L112,200 Z
             M114,200 L124,154 L134,200 Z M136,200 L144,160 L152,200 Z M154,200 L162,157 L170,200 Z
             M172,200 L182,151 L192,200 Z M194,200 L202,159 L210,200 Z M212,200 L220,155 L228,200 Z
             M230,200 L240,149 L250,200 Z M252,200 L260,156 L268,200 Z M270,200 L280,153 L290,200 Z
             M292,200 L300,160 L308,200 Z M310,200 L320,154 L330,200 Z M332,200 L340,158 L348,200 Z
             M350,200 L360,152 L370,200 Z M372,200 L380,157 L388,200 Z M390,200 L400,153 L410,200 Z
             M412,200 L420,158 L428,200 Z M430,200 L440,151 L450,200 Z M452,200 L460,156 L468,200 Z
             M470,200 L480,153 L490,200 Z M492,200 L500,159 L508,200 Z M510,200 L520,154 L530,200 Z
             M532,200 L540,157 L548,200 Z M550,200 L560,151 L570,200 Z M572,200 L580,156 L588,200 Z
             M590,200 L600,153 L610,200 Z M612,200 L620,158 L628,200 Z M630,200 L640,154 L650,200 Z
             M652,200 L660,157 L668,200 Z M670,200 L680,152 L690,200 Z M692,200 L700,156 L708,200 Z
             M710,200 L720,153 L730,200 Z M732,200 L740,158 L748,200 Z M750,200 L760,154 L770,200 Z
             M772,200 L782,157 L792,200 Z M794,200 L800,160 L806,200 Z"
          fill="#060809"
        />

        {/* Nebel-Overlay an der Basis */}
        <rect x="0" y="130" width="800" height="70" fill="url(#fogGrad)" />
      </svg>
    </div>
  )
}
```

### 2d: Blitz-Effekte — Dramatische SVG-Blitze

- [ ] **Step 6: LightningEffect.tsx ersetzen mit SVG-Blitzen**

Ersetze den kompletten Inhalt von `src/components/effects/LightningEffect.tsx`:

```typescript
'use client'

import { useState, useEffect, useCallback } from 'react'

interface Bolt {
  id: number
  x: number
  path: string
  opacity: number
  duration: number
}

function generateBoltPath(startX: number): string {
  let x = startX
  let y = 0
  let path = `M ${x} ${y}`
  const steps = 8 + Math.floor(Math.random() * 6)
  for (let i = 0; i < steps; i++) {
    y += 80 + Math.random() * 60
    x += (Math.random() - 0.5) * 60
    path += ` L ${x} ${y}`
  }
  return path
}

export function LightningEffect() {
  const [bolts, setBolts] = useState<Bolt[]>([])
  const [flash, setFlash] = useState(false)

  const triggerStrike = useCallback(() => {
    const count = Math.random() < 0.3 ? 2 : 1
    const newBolts: Bolt[] = Array.from({ length: count }, (_, i) => ({
      id: Date.now() + i,
      x: 10 + Math.random() * 80,
      path: generateBoltPath(10 + Math.random() * 80),
      opacity: 0.6 + Math.random() * 0.4,
      duration: 120 + Math.random() * 80,
    }))

    setFlash(true)
    setBolts(newBolts)

    setTimeout(() => {
      setFlash(false)
      setBolts([])
    }, newBolts[0].duration + 100)

    // Double-flash für manche Blitze
    if (Math.random() < 0.4) {
      setTimeout(() => {
        setBolts(newBolts.map(b => ({ ...b, opacity: b.opacity * 0.6 })))
        setFlash(true)
      }, newBolts[0].duration + 150)
      setTimeout(() => {
        setFlash(false)
        setBolts([])
      }, newBolts[0].duration + 280)
    }
  }, [])

  useEffect(() => {
    // Unregelmäßige Blitz-Intervalle: 8–35 Sekunden
    function schedule() {
      const delay = 8000 + Math.random() * 27000
      return setTimeout(() => {
        triggerStrike()
        schedule()
      }, delay)
    }
    const timer = schedule()
    return () => clearTimeout(timer)
  }, [triggerStrike])

  return (
    <>
      {/* Screen-Flash */}
      {flash && (
        <div
          className="pointer-events-none fixed inset-0 z-50"
          style={{ background: 'rgba(180, 210, 255, 0.07)' }}
          aria-hidden
        />
      )}

      {/* SVG-Blitze */}
      {bolts.length > 0 && (
        <svg
          className="pointer-events-none fixed inset-0 w-full h-full z-50"
          viewBox="0 0 100 100"
          preserveAspectRatio="xMidYMid slice"
          aria-hidden
        >
          {bolts.map(bolt => (
            <path
              key={bolt.id}
              d={bolt.path}
              stroke="rgba(200, 220, 255, 0.9)"
              strokeWidth="0.3"
              fill="none"
              opacity={bolt.opacity}
              filter="drop-shadow(0 0 2px rgba(180, 210, 255, 0.8))"
            />
          ))}
        </svg>
      )}
    </>
  )
}
```

- [ ] **Step 7: Build + Verify**

```bash
npm run build 2>&1 | tail -20
```

Expected: No TypeScript errors.

- [ ] **Step 8: Commit**

```bash
git add src/app/layout.tsx src/app/globals.css src/app/auth/login/page.tsx src/components/effects/
git commit -m "feat: design overhaul — Cinzel Decorative, hourglass redesign, mountain SVG, dynamic lightning bolts"
```

---

## Task 3: butterfly-effects.ts — Deutsche Übersetzungen + Szenen-Metadaten

**Files:**
- Modify: `src/lib/data/butterfly-effects.ts`

**Wichtig:** Die englischen `options`-Strings bleiben als DB-Keys erhalten (`chosen_option` in Supabase). Neue Felder `nameDe`, `descriptionDe`, `optionsDe` werden für die Anzeige verwendet. Bestehende Saves bleiben gültig.

- [ ] **Step 1: ButterflyEffect Interface erweitern + alle 22 Einträge übersetzen**

Ersetze den kompletten Inhalt von `src/lib/data/butterfly-effects.ts`:

```typescript
export type CharacterName =
  | 'Sam' | 'Mike' | 'Ashley' | 'Chris' | 'Josh' | 'Jessica' | 'Emily' | 'Matt'

export const CHARACTERS: CharacterName[] = [
  'Sam', 'Mike', 'Ashley', 'Chris', 'Josh', 'Jessica', 'Emily', 'Matt',
]

export interface ButterflyEffect {
  id: number
  chapter: number
  name: string
  nameDe: string
  character: CharacterName | CharacterName[]
  description: string
  descriptionDe: string
  options: [string, string]
  optionsDe: [string, string]
  isTimed: boolean
  sceneImage: string
}

export const BUTTERFLY_EFFECTS: ButterflyEffect[] = [
  {
    id: 1,
    chapter: 0,
    name: 'The Prank',
    nameDe: 'Der Streich',
    character: ['Mike', 'Ashley', 'Chris', 'Emily', 'Matt', 'Jessica'],
    description: 'The group decides to prank Hannah. Who orchestrates it?',
    descriptionDe: 'Die Gruppe beschließt, Hannah einen Streich zu spielen. Wer steckt dahinter?',
    options: ['Mike leads the prank', 'Emily leads the prank'],
    optionsDe: ['Mike führt den Streich an', 'Emily führt den Streich an'],
    isTimed: false,
    sceneImage: '01-the-prank.jpg',
  },
  {
    id: 2,
    chapter: 1,
    name: "Sam's Exploration",
    nameDe: 'Sams Erkundung',
    character: 'Sam',
    description: 'Sam arrives at the lodge. Does she explore or head straight to the bath?',
    descriptionDe: 'Sam kommt in der Hütte an. Erkundet sie das Gebäude oder geht sie direkt ins Bad?',
    options: ['Explore the lodge', 'Go to the bath'],
    optionsDe: ['Die Hütte erkunden', 'Ins Bad gehen'],
    isTimed: false,
    sceneImage: '02-sams-exploration.jpg',
  },
  {
    id: 3,
    chapter: 2,
    name: 'The Ouija Board',
    nameDe: 'Das Ouija-Brett',
    character: 'Ashley',
    description: 'Ashley uses the ouija board. Does she ask about the twins?',
    descriptionDe: 'Ashley benutzt das Ouija-Brett. Fragt sie nach den Zwillingen?',
    options: ['Ask about the twins', 'Refuse to continue'],
    optionsDe: ['Nach den Zwillingen fragen', 'Aufhören'],
    isTimed: false,
    sceneImage: '03-ouija-board.jpg',
  },
  {
    id: 4,
    chapter: 2,
    name: "Matt's Flare Gun",
    nameDe: 'Matts Leuchtpistole',
    character: 'Matt',
    description: 'Matt finds a flare gun in the shed. Does he take it?',
    descriptionDe: 'Matt findet eine Leuchtpistole im Schuppen. Nimmt er sie mit?',
    options: ['Take the flare gun', 'Leave it behind'],
    optionsDe: ['Leuchtpistole mitnehmen', 'Zurücklassen'],
    isTimed: false,
    sceneImage: '04-flare-gun.jpg',
  },
  {
    id: 5,
    chapter: 3,
    name: 'The Letter',
    nameDe: 'Der Brief',
    character: 'Ashley',
    description: "Ashley finds a letter implicating Mike in Hannah's prank. Does she read it aloud?",
    descriptionDe: 'Ashley findet einen Brief, der Mikes Rolle im Streich belegt. Liest sie ihn vor?',
    options: ['Read it to Chris', 'Keep it to herself'],
    optionsDe: ['Chris vorlesen', 'Für sich behalten'],
    isTimed: false,
    sceneImage: '05-the-letter.jpg',
  },
  {
    id: 6,
    chapter: 3,
    name: 'The Wolf',
    nameDe: 'Der Wolf',
    character: 'Mike',
    description: 'A wolf attacks Mike on the path. Does he shoot it?',
    descriptionDe: 'Ein Wolf greift Mike auf dem Weg an. Schießt er?',
    options: ['Shoot the wolf', 'Spare the wolf'],
    optionsDe: ['Den Wolf erschießen', 'Den Wolf verschonen'],
    isTimed: true,
    sceneImage: '06-the-wolf.jpg',
  },
  {
    id: 7,
    chapter: 4,
    name: "Jessica's Rescue",
    nameDe: 'Jessicas Rettung',
    character: 'Mike',
    description: 'Mike chases after Jessica through the woods. Does he reach her in time?',
    descriptionDe: 'Mike verfolgt Jessica durch den Wald. Erreicht er sie rechtzeitig?',
    options: ['Reach her in time', 'Arrive too late'],
    optionsDe: ['Rechtzeitig ankommen', 'Zu spät kommen'],
    isTimed: true,
    sceneImage: '07-jessicas-rescue.jpg',
  },
  {
    id: 8,
    chapter: 4,
    name: 'The Fire Tower',
    nameDe: 'Der Feuerwachturm',
    character: 'Emily',
    description: 'Emily and Matt reach the fire tower. Does Emily take the flare gun from Matt?',
    descriptionDe: 'Emily und Matt erreichen den Feuerwachturm. Nimmt Emily die Leuchtpistole?',
    options: ['Emily takes the flare gun', 'Matt keeps the flare gun'],
    optionsDe: ['Emily nimmt die Leuchtpistole', 'Matt behält die Leuchtpistole'],
    isTimed: false,
    sceneImage: '08-fire-tower.jpg',
  },
  {
    id: 9,
    chapter: 5,
    name: "Chris's Honesty",
    nameDe: 'Chris\' Ehrlichkeit',
    character: 'Chris',
    description: 'Chris tells Ashley what the ouija board revealed about the twins.',
    descriptionDe: 'Chris erzählt Ashley, was das Ouija-Brett über die Zwillinge enthüllt hat.',
    options: ['Tell Ashley the truth', 'Keep the secret'],
    optionsDe: ['Ashley die Wahrheit sagen', 'Das Geheimnis bewahren'],
    isTimed: false,
    sceneImage: '09-chris-honesty.jpg',
  },
  {
    id: 10,
    chapter: 5,
    name: 'The Clue',
    nameDe: 'Der Hinweis',
    character: 'Sam',
    description: 'Sam discovers a clue about what really happened to the twins a year ago.',
    descriptionDe: 'Sam entdeckt einen Hinweis darauf, was wirklich vor einem Jahr mit den Zwillingen geschah.',
    options: ['Investigate further', 'Leave it alone'],
    optionsDe: ['Weiter nachforschen', 'Es auf sich beruhen lassen'],
    isTimed: false,
    sceneImage: '10-the-clue.jpg',
  },
  {
    id: 11,
    chapter: 6,
    name: 'The Bear Trap',
    nameDe: 'Die Bärenfalle',
    character: ['Chris', 'Ashley'],
    description: 'Chris and Ashley are trapped. Chris must shoot one of them to survive.',
    descriptionDe: 'Chris und Ashley sitzen in der Falle. Chris muss eine Person erschießen, um zu überleben.',
    options: ['Shoot Ashley', 'Shoot himself'],
    optionsDe: ['Ashley erschießen', 'Sich selbst erschießen'],
    isTimed: true,
    sceneImage: '11-bear-trap.jpg',
  },
  {
    id: 12,
    chapter: 6,
    name: "Ashley's Door",
    nameDe: 'Ashleys Tür',
    character: 'Ashley',
    description: "Chris is banging on the door after escaping the trap. Does Ashley open it?",
    descriptionDe: 'Chris hämmert nach seiner Flucht aus der Falle an die Tür. Öffnet Ashley?',
    options: ['Open the door', 'Leave Chris outside'],
    optionsDe: ['Tür öffnen', 'Chris draußen lassen'],
    isTimed: true,
    sceneImage: '12-ashleys-door.jpg',
  },
  {
    id: 13,
    chapter: 7,
    name: "Mike's Fingers",
    nameDe: 'Mikes Finger',
    character: 'Mike',
    description: 'Mike finds his fingers caught in a bear trap in the sanatorium.',
    descriptionDe: 'Mike findet seine Finger in einer Bärenfalle in der Irrenanstalt eingeklemmt.',
    options: ['Amputate fingers', 'Leave fingers trapped'],
    optionsDe: ['Finger amputieren', 'Finger eingeklemmt lassen'],
    isTimed: true,
    sceneImage: '13-mikes-fingers.jpg',
  },
  {
    id: 14,
    chapter: 7,
    name: 'The Mines Escape',
    nameDe: 'Flucht durch die Minen',
    character: 'Emily',
    description: 'Emily tries to escape through the mines. Does she outrun the threat?',
    descriptionDe: 'Emily versucht, durch die Minen zu fliehen. Kann sie der Bedrohung entkommen?',
    options: ['Outrun the threat', 'Gets bitten'],
    optionsDe: ['Der Bedrohung entkommen', 'Gebissen werden'],
    isTimed: true,
    sceneImage: '14-mines-escape.jpg',
  },
  {
    id: 15,
    chapter: 8,
    name: "Emily's Bite",
    nameDe: 'Emilys Biss',
    character: 'Mike',
    description: 'Emily reveals she was bitten. Mike must decide her fate.',
    descriptionDe: 'Emily offenbart, dass sie gebissen wurde. Mike entscheidet über ihr Schicksal.',
    options: ['Shoot Emily', 'Spare Emily'],
    optionsDe: ['Emily erschießen', 'Emily verschonen'],
    isTimed: true,
    sceneImage: '15-emilys-bite.jpg',
  },
  {
    id: 16,
    chapter: 8,
    name: "Sam's Discovery",
    nameDe: 'Sams Entdeckung',
    character: 'Sam',
    description: "Sam discovers the full truth about Josh's plan for the night.",
    descriptionDe: 'Sam entdeckt die ganze Wahrheit über Joshs Plan für diese Nacht.',
    options: ['Confront Josh directly', 'Gather more evidence first'],
    optionsDe: ['Josh direkt konfrontieren', 'Erst mehr Beweise sammeln'],
    isTimed: false,
    sceneImage: '16-sams-discovery.jpg',
  },
  {
    id: 17,
    chapter: 9,
    name: "Josh's Fate",
    nameDe: 'Joshs Schicksal',
    character: 'Sam',
    description: "Sam must decide what to do about Josh's situation in the sanatorium.",
    descriptionDe: 'Sam muss entscheiden, was mit Joshs Situation in der Irrenanstalt geschehen soll.',
    options: ['Try to save Josh', 'Prioritize escaping'],
    optionsDe: ['Josh zu retten versuchen', 'Flucht priorisieren'],
    isTimed: false,
    sceneImage: '17-joshs-fate.jpg',
  },
  {
    id: 18,
    chapter: 9,
    name: 'The Mines Path',
    nameDe: 'Der Minenweg',
    character: 'Mike',
    description: 'Mike encounters something in the mines. Does he fight or flee?',
    descriptionDe: 'Mike begegnet etwas in den Minen. Kämpft er oder flieht er?',
    options: ['Stand and fight', 'Run'],
    optionsDe: ['Standhalten und kämpfen', 'Fliehen'],
    isTimed: true,
    sceneImage: '18-mines-path.jpg',
  },
  {
    id: 19,
    chapter: 10,
    name: 'The Basement',
    nameDe: 'Der Keller',
    character: ['Sam', 'Mike'],
    description: 'Sam and Mike reunite. Do they try to rescue the others or head for safety?',
    descriptionDe: 'Sam und Mike treffen sich wieder. Retten sie die anderen oder suchen sie Schutz?',
    options: ['Try to rescue others', 'Head for the exit'],
    optionsDe: ['Die anderen retten', 'Zum Ausgang'],
    isTimed: false,
    sceneImage: '19-the-basement.jpg',
  },
  {
    id: 20,
    chapter: 10,
    name: 'The Generator',
    nameDe: 'Der Generator',
    character: 'Sam',
    description: 'Sam must decide when to activate the generator switch.',
    descriptionDe: 'Sam muss entscheiden, wann sie den Generatorschalter aktiviert.',
    options: ['Activate immediately', 'Wait for better timing'],
    optionsDe: ['Sofort aktivieren', 'Auf besseren Moment warten'],
    isTimed: true,
    sceneImage: '20-the-generator.jpg',
  },
  {
    id: 21,
    chapter: 10,
    name: 'The Sacrifice',
    nameDe: 'Das Opfer',
    character: 'Sam',
    description: 'Sam has a chance to end everything — at great personal risk.',
    descriptionDe: 'Sam hat die Chance, alles zu beenden — mit großem persönlichem Risiko.',
    options: ['Blow it all up', 'Find another way'],
    optionsDe: ['Alles in die Luft jagen', 'Einen anderen Weg finden'],
    isTimed: true,
    sceneImage: '21-the-sacrifice.jpg',
  },
  {
    id: 22,
    chapter: 10,
    name: 'Until Dawn',
    nameDe: 'Until Dawn',
    character: ['Sam', 'Mike', 'Ashley', 'Chris', 'Emily', 'Matt'],
    description: 'The rescue helicopter arrives. Who survives until dawn?',
    descriptionDe: 'Der Rettungshubschrauber trifft ein. Wer überlebt bis zum Morgengrauen?',
    options: ['Multiple survivors', 'Solo survivor'],
    optionsDe: ['Mehrere Überlebende', 'Einziger Überlebender'],
    isTimed: false,
    sceneImage: '22-until-dawn.jpg',
  },
]
```

- [ ] **Step 2: Verify TypeScript**

```bash
npx tsc --noEmit 2>&1 | head -30
```

Expected: No errors (or only unrelated existing errors).

- [ ] **Step 3: Commit**

```bash
git add src/lib/data/butterfly-effects.ts
git commit -m "feat: butterfly effects — deutsche Übersetzungen, sceneImage-Feld"
```

---

## Task 4: Karten-UX — ButterflyCard + CardNavigator

**Files:**
- Create: `public/scenes/` (22 Szenenbilder)
- Create: `src/components/decisions/ButterflyCard.tsx`
- Create: `src/components/decisions/CardNavigator.tsx`
- Modify: `src/app/runs/[id]/page.tsx`
- Delete: `src/components/decisions/DecisionCard.tsx`
- Delete: `src/components/decisions/DecisionList.tsx`
- Delete: `src/components/decisions/TimedDecision.tsx`
- Delete: `src/components/decisions/TimedModeButton.tsx`

### 4a: Szenenbilder

- [ ] **Step 1: Szenen-Verzeichnis anlegen + Bilder suchen und herunterladen**

```bash
mkdir -p /Users/sebastianprehmus/Workspace/until-dawn-tracker/public/scenes
```

Claude recherchiert via WebSearch für jede der 22 Szenen ein passendes Screenshot aus dem Spiel (Until Dawn Wiki, Fanbases, YouTube Thumbnails). Gesucht wird nach Gameplay-Screenshots die die jeweilige Szene zeigen. Die Bilder werden per `curl` heruntergeladen:

```bash
# Beispiel-Muster:
curl -L "IMAGE_URL" -o /Users/sebastianprehmus/Workspace/until-dawn-tracker/public/scenes/01-the-prank.jpg
```

Falls für eine Szene kein geeignetes Bild gefunden wird, wird eine CSS-Fallback-Gradient als Default verwendet (kein Bild nötig — das wird im ButterflyCard-Komponenten gehandhabt).

Recherche-Ziele (22 Szenen):
- `01-the-prank.jpg` — Gruppe beim Pranken von Hannah, Lodge-Szene
- `02-sams-exploration.jpg` — Sam erkundet die Hütte
- `03-ouija-board.jpg` — Ashley am Ouija-Brett
- `04-flare-gun.jpg` — Matt findet Leuchtpistole im Schuppen
- `05-the-letter.jpg` — Ashley liest Brief
- `06-the-wolf.jpg` — Mike begegnet dem Wolf
- `07-jessicas-rescue.jpg` — Mike verfolgt Jessica durch Wald
- `08-fire-tower.jpg` — Emily und Matt am Feuerwachturm
- `09-chris-honesty.jpg` — Chris spricht mit Ashley
- `10-the-clue.jpg` — Sam entdeckt Hinweis
- `11-bear-trap.jpg` — Chris und Ashley in der Bärenfalle
- `12-ashleys-door.jpg` — Ashley an der Tür
- `13-mikes-fingers.jpg` — Mike in der Irrenanstalt
- `14-mines-escape.jpg` — Emily flieht durch Minen
- `15-emilys-bite.jpg` — Emily zeigt ihren Biss
- `16-sams-discovery.jpg` — Sam entdeckt die Wahrheit über Josh
- `17-joshs-fate.jpg` — Sam und Josh in der Irrenanstalt
- `18-mines-path.jpg` — Mike in den Minen
- `19-the-basement.jpg` — Sam und Mike im Keller
- `20-the-generator.jpg` — Sam am Generatorschalter
- `21-the-sacrifice.jpg` — Sam vor der Explosion
- `22-until-dawn.jpg` — Morgengrauen, Helikopter

### 4b: ButterflyCard Komponente

- [ ] **Step 2: ButterflyCard.tsx erstellen**

Erstelle `src/components/decisions/ButterflyCard.tsx`:

```typescript
'use client'

import { useState } from 'react'
import { createClient } from '@/lib/supabase/client'
import { cn } from '@/lib/utils'
import type { ButterflyEffect } from '@/lib/data/butterfly-effects'
import { Clock } from 'lucide-react'

interface OtherRunChoice {
  runName: string
  choice: string | undefined
}

interface ButterflyCardProps {
  effect: ButterflyEffect
  runId: string
  existingChoice: string | undefined
  otherRunChoices: OtherRunChoice[]
  onChoiceChanged: (effectName: string, choice: string | null) => void
}

const CHAPTER_GRADIENTS: Record<number, string> = {
  0:  'from-[#1a0a0a] via-[#0d0508] to-[#050505]',
  1:  'from-[#0a0e1a] via-[#080c14] to-[#050505]',
  2:  'from-[#0f0f1a] via-[#0a0a14] to-[#050505]',
  3:  'from-[#1a0f08] via-[#140c06] to-[#050505]',
  4:  'from-[#0a1a10] via-[#08140c] to-[#050505]',
  5:  'from-[#1a1408] via-[#141006] to-[#050505]',
  6:  'from-[#1a0808] via-[#140606] to-[#050505]',
  7:  'from-[#120a1a] via-[#0e0814] to-[#050505]',
  8:  'from-[#1a0a0a] via-[#0d0508] to-[#050505]',
  9:  'from-[#08141a] via-[#061014] to-[#050505]',
  10: 'from-[#1a0808] via-[#140606] to-[#050505]',
}

export function ButterflyCard({ effect, runId, existingChoice, otherRunChoices, onChoiceChanged }: ButterflyCardProps) {
  const [chosen, setChosen] = useState<string | undefined>(existingChoice)
  const [loading, setLoading] = useState(false)
  const supabase = createClient()

  async function handleOption(option: string) {
    if (loading) return

    // Undo: erneuter Klick auf gewählte Option → Auswahl aufheben
    if (chosen === option) {
      setLoading(true)
      setChosen(undefined)
      onChoiceChanged(effect.name, null)

      await supabase
        .from('decisions')
        .delete()
        .eq('run_id', runId)
        .eq('butterfly_effect_name', effect.name)

      setLoading(false)
      return
    }

    setLoading(true)
    setChosen(option)
    onChoiceChanged(effect.name, option)

    const { data: existing } = await supabase
      .from('decisions')
      .select('id')
      .eq('run_id', runId)
      .eq('butterfly_effect_name', effect.name)
      .maybeSingle()

    if (existing) {
      await supabase.from('decisions').delete().eq('id', existing.id)
    }

    await supabase.from('decisions').insert({
      run_id: runId,
      chapter: effect.chapter,
      butterfly_effect_name: effect.name,
      chosen_option: option,
    })

    setLoading(false)
  }

  const gradient = CHAPTER_GRADIENTS[effect.chapter] ?? CHAPTER_GRADIENTS[0]
  const chapterLabel = effect.chapter === 0 ? 'Prolog' : `Kapitel ${effect.chapter}`

  return (
    <div className="rounded-xl overflow-hidden border border-horror-border shadow-2xl">
      {/* Szenen-Bild oder Gradient-Fallback */}
      <div className="relative h-48 overflow-hidden">
        <img
          src={`/scenes/${effect.sceneImage}`}
          alt=""
          className="w-full h-full object-cover"
          onError={(e) => {
            (e.target as HTMLImageElement).style.display = 'none'
          }}
        />
        <div className={cn('absolute inset-0 bg-gradient-to-b', gradient)} />
        <div className="absolute inset-0 bg-gradient-to-b from-transparent via-transparent to-horror-bg" />

        {/* Kapitel + Szenenname */}
        <div className="absolute bottom-0 left-0 right-0 p-4">
          <div className="flex items-center gap-2 mb-1">
            <span className="text-xs text-horror-muted tracking-widest uppercase font-cinzel">
              {chapterLabel}
            </span>
            {effect.isTimed && (
              <span className="flex items-center gap-1 text-xs font-bold text-horror-accent tracking-widest">
                <Clock size={10} /> ZEITENTSCHEIDUNG
              </span>
            )}
          </div>
          <h2 className="font-cinzel-decorative text-xl font-bold text-horror-text">
            {effect.nameDe}
          </h2>
        </div>
      </div>

      {/* Karten-Inhalt */}
      <div className="bg-horror-card p-4">
        {/* Beschreibung */}
        <p className="text-sm text-horror-muted mb-4 leading-relaxed">
          {effect.descriptionDe}
        </p>

        {/* Andere Runs — Badges */}
        {otherRunChoices.some(r => r.choice) && (
          <div className="flex flex-wrap gap-2 mb-4">
            {otherRunChoices
              .filter(r => r.choice)
              .map(r => {
                const isOption1 = r.choice === effect.options[0]
                return (
                  <span
                    key={r.runName}
                    className={cn(
                      'text-[10px] font-semibold px-2 py-1 rounded-full border tracking-wide',
                      isOption1
                        ? 'bg-[#0d2030] text-[#7db8cc] border-[#2a5a7a]'
                        : 'bg-[#1a0a0a] text-[#cc7d7d] border-[#4a2020]'
                    )}
                  >
                    {r.runName}: {isOption1 ? effect.optionsDe[0] : effect.optionsDe[1]}
                  </span>
                )
              })}
          </div>
        )}

        {/* Optionen */}
        <div className="grid grid-cols-2 gap-3">
          {effect.optionsDe.map((label, i) => {
            const dbOption = effect.options[i]
            const isChosen = chosen === dbOption
            return (
              <button
                key={dbOption}
                onClick={() => handleOption(dbOption)}
                disabled={loading}
                className={cn(
                  'rounded-lg py-4 px-3 text-sm font-semibold transition-all duration-150 active:scale-95 tap-target text-center leading-tight',
                  isChosen
                    ? 'bg-horror-accent text-white border border-horror-accent shadow-lg shadow-red-900/30'
                    : 'bg-horror-bg border border-horror-border text-horror-muted hover:border-horror-accent hover:text-horror-text'
                )}
              >
                {label}
                {isChosen && (
                  <span className="block text-[10px] mt-1 opacity-70">Tippen zum Rückgängig</span>
                )}
              </button>
            )
          })}
        </div>
      </div>
    </div>
  )
}
```

### 4c: CardNavigator Komponente

- [ ] **Step 3: CardNavigator.tsx erstellen**

Erstelle `src/components/decisions/CardNavigator.tsx`:

```typescript
'use client'

import { useState, useRef, useCallback, useEffect } from 'react'
import { ChevronLeft, ChevronRight } from 'lucide-react'
import { BUTTERFLY_EFFECTS } from '@/lib/data/butterfly-effects'
import { ButterflyCard } from './ButterflyCard'
import { cn } from '@/lib/utils'

interface OtherRun {
  runId: string
  runName: string
  choices: Record<string, string>
}

interface CardNavigatorProps {
  runId: string
  initialDecisions: Array<{ butterfly_effect_name: string; chosen_option: string }>
  otherRuns: OtherRun[]
}

export function CardNavigator({ runId, initialDecisions, otherRuns }: CardNavigatorProps) {
  const [index, setIndex] = useState(0)
  const [slideDir, setSlideDir] = useState<'in-left' | 'in-right' | null>(null)
  const [decisions, setDecisions] = useState<Record<string, string>>(
    Object.fromEntries(initialDecisions.map(d => [d.butterfly_effect_name, d.chosen_option]))
  )

  const touchStartX = useRef<number | null>(null)
  const touchStartY = useRef<number | null>(null)
  const total = BUTTERFLY_EFFECTS.length

  function navigate(newIndex: number, direction: 'forward' | 'back') {
    if (newIndex < 0 || newIndex >= total) return
    setSlideDir(direction === 'forward' ? 'in-left' : 'in-right')
    setTimeout(() => {
      setIndex(newIndex)
      setSlideDir(null)
    }, 180)
  }

  function goNext() { navigate(index + 1, 'forward') }
  function goPrev() { navigate(index - 1, 'back') }

  function handleTouchStart(e: React.TouchEvent) {
    touchStartX.current = e.touches[0].clientX
    touchStartY.current = e.touches[0].clientY
  }

  function handleTouchEnd(e: React.TouchEvent) {
    if (touchStartX.current === null || touchStartY.current === null) return
    const dx = e.changedTouches[0].clientX - touchStartX.current
    const dy = e.changedTouches[0].clientY - touchStartY.current
    if (Math.abs(dx) > Math.abs(dy) && Math.abs(dx) > 44) {
      if (dx < 0) goNext(); else goPrev()
    }
    touchStartX.current = null
    touchStartY.current = null
  }

  const handleKeyDown = useCallback((e: KeyboardEvent) => {
    if (e.key === 'ArrowRight') goNext()
    if (e.key === 'ArrowLeft') goPrev()
  }, [index])

  useEffect(() => {
    window.addEventListener('keydown', handleKeyDown)
    return () => window.removeEventListener('keydown', handleKeyDown)
  }, [handleKeyDown])

  function handleChoiceChanged(effectName: string, choice: string | null) {
    setDecisions(prev => {
      const next = { ...prev }
      if (choice === null) delete next[effectName]; else next[effectName] = choice
      return next
    })
  }

  const effect = BUTTERFLY_EFFECTS[index]
  const completedCount = Object.keys(decisions).length
  const chapterLabel = effect.chapter === 0 ? 'Prolog' : `Kapitel ${effect.chapter}`

  return (
    <div>
      {/* Progress Bar + Stats */}
      <div className="flex items-center justify-between mb-3">
        <span className="text-xs text-horror-muted font-cinzel tracking-widest uppercase">
          {chapterLabel}
        </span>
        <span className="text-xs text-horror-muted">
          {index + 1} / {total}
          {completedCount > 0 && (
            <span className="ml-2 text-horror-accent">{completedCount} entschieden</span>
          )}
        </span>
      </div>

      {/* Fortschritts-Dots */}
      <div className="flex gap-0.5 mb-4 overflow-hidden">
        {BUTTERFLY_EFFECTS.map((e, i) => (
          <button
            key={e.id}
            onClick={() => navigate(i, i > index ? 'forward' : 'back')}
            className={cn(
              'h-1 flex-1 rounded-full transition-all duration-200',
              i === index
                ? 'bg-horror-accent'
                : decisions[e.name]
                ? 'bg-green-800'
                : 'bg-horror-border'
            )}
            aria-label={`Szene ${i + 1}: ${e.nameDe}`}
          />
        ))}
      </div>

      {/* Karte */}
      <div
        onTouchStart={handleTouchStart}
        onTouchEnd={handleTouchEnd}
        className={cn(
          'transition-all duration-180',
          slideDir === 'in-left' && 'translate-x-[-16px] opacity-0',
          slideDir === 'in-right' && 'translate-x-[16px] opacity-0',
          !slideDir && 'translate-x-0 opacity-100'
        )}
      >
        <ButterflyCard
          key={effect.id}
          effect={effect}
          runId={runId}
          existingChoice={decisions[effect.name]}
          otherRunChoices={otherRuns.map(r => ({
            runName: r.runName,
            choice: r.choices[effect.name],
          }))}
          onChoiceChanged={handleChoiceChanged}
        />
      </div>

      {/* Navigation */}
      <div className="flex items-center justify-between mt-4">
        <button
          onClick={goPrev}
          disabled={index === 0}
          className={cn(
            'flex items-center gap-2 px-4 py-3 rounded-lg border transition-all tap-target',
            index === 0
              ? 'border-horror-border text-horror-border opacity-30'
              : 'border-horror-border text-horror-muted hover:border-horror-accent hover:text-horror-text'
          )}
        >
          <ChevronLeft size={18} />
          <span className="text-sm">Zurück</span>
        </button>

        <button
          onClick={goNext}
          disabled={index === total - 1}
          className={cn(
            'flex items-center gap-2 px-4 py-3 rounded-lg border transition-all tap-target',
            index === total - 1
              ? 'border-horror-border text-horror-border opacity-30'
              : 'border-horror-accent text-horror-accent hover:bg-horror-accent hover:text-white'
          )}
        >
          <span className="text-sm">Weiter</span>
          <ChevronRight size={18} />
        </button>
      </div>
    </div>
  )
}
```

### 4d: Run-Overview-Seite umbauen

- [ ] **Step 4: runs/[id]/page.tsx umschreiben**

Ersetze den kompletten Inhalt von `src/app/runs/[id]/page.tsx`:

```typescript
import { notFound } from 'next/navigation'
import Link from 'next/link'
import { ArrowLeft, BarChart2 } from 'lucide-react'
import { createClient } from '@/lib/supabase/server'
import { CharacterGrid } from '@/components/characters/CharacterGrid'
import { CardNavigator } from '@/components/decisions/CardNavigator'

export default async function RunPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()

  const [
    { data: run },
    { data: characters },
    { data: decisions },
    { data: allRuns },
  ] = await Promise.all([
    supabase.from('runs').select('id, name').eq('id', id).single(),
    supabase.from('characters').select('id, name, status').eq('run_id', id).order('name'),
    supabase.from('decisions').select('butterfly_effect_name, chosen_option').eq('run_id', id),
    supabase.from('runs').select('id, name').eq('user_id', user!.id).neq('id', id).order('created_at', { ascending: false }),
  ])

  if (!run) notFound()

  // Entscheidungen aller anderen Runs laden
  const otherRunDecisions = await Promise.all(
    (allRuns ?? []).map(async (r) => {
      const { data: otherDecisions } = await supabase
        .from('decisions')
        .select('butterfly_effect_name, chosen_option')
        .eq('run_id', r.id)
      return {
        runId: r.id,
        runName: r.name,
        choices: Object.fromEntries(
          (otherDecisions ?? []).map(d => [d.butterfly_effect_name, d.chosen_option])
        ),
      }
    })
  )

  return (
    <main className="min-h-screen px-4 py-8 max-w-lg mx-auto pb-24">
      {/* Header */}
      <div className="flex items-center justify-between mb-6">
        <div className="flex items-center gap-3">
          <Link href="/runs" className="text-horror-muted tap-target flex items-center">
            <ArrowLeft size={20} />
          </Link>
          <h1 className="font-cinzel text-lg font-bold text-horror-text truncate max-w-[200px]">
            {run.name}
          </h1>
        </div>
        {(allRuns ?? []).length > 0 && (
          <Link
            href={`/runs/${id}/compare`}
            className="flex items-center gap-1.5 text-sm text-horror-muted border border-horror-border rounded-lg px-3 py-2 hover:border-horror-accent tap-target"
          >
            <BarChart2 size={14} /> Vergleich
          </Link>
        )}
      </div>

      {/* Charakterstatus */}
      <section className="mb-8">
        <h2 className="text-xs font-bold tracking-widest text-horror-muted uppercase mb-3 font-cinzel">
          Charaktere
        </h2>
        <CharacterGrid characters={(characters ?? []) as Array<{ id: string; name: string; status: 'alive' | 'dead' | 'unknown' }>} />
      </section>

      {/* Karten-Navigation */}
      <section>
        <h2 className="text-xs font-bold tracking-widest text-horror-muted uppercase mb-4 font-cinzel">
          Schmetterlingseffekte
        </h2>
        <CardNavigator
          runId={id}
          initialDecisions={decisions ?? []}
          otherRuns={otherRunDecisions}
        />
      </section>
    </main>
  )
}
```

- [ ] **Step 5: Alte Komponenten löschen**

```bash
rm src/components/decisions/DecisionCard.tsx
rm src/components/decisions/DecisionList.tsx
rm src/components/decisions/TimedDecision.tsx
rm src/components/decisions/TimedModeButton.tsx
rm src/components/comparison/ComparisonView.tsx
```

- [ ] **Step 6: TypeScript-Build verifizieren**

```bash
npx tsc --noEmit 2>&1 | head -40
```

Expected: Keine Fehler mehr.

- [ ] **Step 7: Commit**

```bash
git add src/ public/scenes/
git commit -m "feat: card-by-card UX — swipeable ButterflyCard + CardNavigator, timed mode removed, comparison badges in cards"
```

---

## Task 5: Run-Zeitstrahl (Compare-Seite)

**Files:**
- Create: `src/components/comparison/RunTimeline.tsx`
- Modify: `src/app/runs/[id]/compare/page.tsx`

- [ ] **Step 1: RunTimeline.tsx erstellen**

Erstelle `src/components/comparison/RunTimeline.tsx`:

```typescript
'use client'

import { BUTTERFLY_EFFECTS } from '@/lib/data/butterfly-effects'
import { cn } from '@/lib/utils'
import { useState } from 'react'

interface RunData {
  id: string
  name: string
  choices: Record<string, string>
}

interface RunTimelineProps {
  currentRun: RunData
  otherRuns: RunData[]
}

export function RunTimeline({ currentRun, otherRuns }: RunTimelineProps) {
  const [expandedEffect, setExpandedEffect] = useState<number | null>(null)
  const allRuns = [currentRun, ...otherRuns]

  if (otherRuns.length === 0) {
    return (
      <div className="text-center py-16">
        <p className="text-horror-muted">Du brauchst mindestens 2 Runs für einen Vergleich.</p>
        <p className="text-horror-muted text-sm mt-1">Erstelle einen weiteren Run, um Unterschiede zu sehen.</p>
      </div>
    )
  }

  const chapters = [...new Set(BUTTERFLY_EFFECTS.map(e => e.chapter))].sort((a, b) => a - b)

  return (
    <div>
      {/* Run-Legende */}
      <div className="flex flex-wrap gap-2 mb-6">
        {allRuns.map((run, i) => (
          <div key={run.id} className="flex items-center gap-1.5">
            <div className={cn('w-3 h-3 rounded-full', i === 0 ? 'bg-horror-accent' : 'bg-[#7db8cc]')} />
            <span className="text-xs text-horror-muted">{run.name}</span>
          </div>
        ))}
      </div>

      {/* Zeitstrahl pro Kapitel */}
      {chapters.map(ch => {
        const effects = BUTTERFLY_EFFECTS.filter(e => e.chapter === ch)
        const chapterLabel = ch === 0 ? 'Prolog' : `Kapitel ${ch}`

        return (
          <div key={ch} className="mb-6">
            <h3 className="text-xs font-bold tracking-widest text-horror-muted uppercase mb-2 font-cinzel">
              {chapterLabel}
            </h3>

            <div className="space-y-1">
              {effects.map(effect => {
                const choices = allRuns.map(r => r.choices[effect.name])
                const anyChosen = choices.some(Boolean)
                const hasDivergence = anyChosen &&
                  choices.filter(Boolean).length > 1 &&
                  new Set(choices.filter(Boolean)).size > 1

                const isExpanded = expandedEffect === effect.id

                return (
                  <button
                    key={effect.id}
                    onClick={() => setExpandedEffect(isExpanded ? null : effect.id)}
                    className={cn(
                      'w-full text-left rounded-lg border px-3 py-2.5 transition-all',
                      hasDivergence
                        ? 'border-[#4a3800] bg-[#1a1400]'
                        : anyChosen
                        ? 'border-horror-border bg-horror-card'
                        : 'border-horror-border/40 bg-transparent opacity-50'
                    )}
                  >
                    <div className="flex items-center justify-between">
                      <span className={cn(
                        'text-sm font-semibold',
                        hasDivergence ? 'text-[#c9a84c]' : 'text-horror-text'
                      )}>
                        {effect.nameDe}
                        {hasDivergence && ' ⚡'}
                      </span>

                      {/* Run-Dots */}
                      <div className="flex gap-1.5 ml-2 shrink-0">
                        {allRuns.map((run, i) => {
                          const choice = run.choices[effect.name]
                          const isOpt1 = choice === effect.options[0]
                          return (
                            <div
                              key={run.id}
                              className={cn(
                                'w-2.5 h-2.5 rounded-full',
                                !choice && 'bg-horror-border opacity-40',
                                choice && isOpt1 && (i === 0 ? 'bg-horror-accent' : 'bg-[#7db8cc]'),
                                choice && !isOpt1 && (i === 0 ? 'bg-red-900' : 'bg-[#4a6a7a]')
                              )}
                              title={choice ?? 'Nicht entschieden'}
                            />
                          )
                        })}
                      </div>
                    </div>

                    {/* Expandierte Details */}
                    {isExpanded && anyChosen && (
                      <div className="mt-2 pt-2 border-t border-horror-border/50 space-y-1">
                        {allRuns.map((run, i) => {
                          const choice = run.choices[effect.name]
                          if (!choice) return null
                          const isOpt1 = choice === effect.options[0]
                          const label = isOpt1 ? effect.optionsDe[0] : effect.optionsDe[1]
                          return (
                            <div key={run.id} className="flex items-center gap-2">
                              <div className={cn(
                                'w-2 h-2 rounded-full shrink-0',
                                i === 0 ? 'bg-horror-accent' : 'bg-[#7db8cc]'
                              )} />
                              <span className="text-xs text-horror-muted">{run.name}:</span>
                              <span className="text-xs text-horror-text">{label}</span>
                            </div>
                          )
                        })}
                      </div>
                    )}
                  </button>
                )
              })}
            </div>
          </div>
        )
      })}
    </div>
  )
}
```

- [ ] **Step 2: compare/page.tsx umschreiben**

Ersetze den kompletten Inhalt von `src/app/runs/[id]/compare/page.tsx`:

```typescript
import { createClient } from '@/lib/supabase/server'
import { notFound } from 'next/navigation'
import Link from 'next/link'
import { ArrowLeft } from 'lucide-react'
import { RunTimeline } from '@/components/comparison/RunTimeline'

export default async function ComparePage({
  params,
}: {
  params: Promise<{ id: string }>
}) {
  const { id } = await params
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()

  const [{ data: currentRun }, { data: allRuns }, { data: currentDecisions }] = await Promise.all([
    supabase.from('runs').select('id, name').eq('id', id).eq('user_id', user!.id).single(),
    supabase.from('runs').select('id, name').eq('user_id', user!.id).neq('id', id).order('created_at', { ascending: false }),
    supabase.from('decisions').select('butterfly_effect_name, chosen_option').eq('run_id', id),
  ])

  if (!currentRun) notFound()

  const otherRunsWithDecisions = await Promise.all(
    (allRuns ?? []).map(async (r) => {
      const { data: decisions } = await supabase
        .from('decisions')
        .select('butterfly_effect_name, chosen_option')
        .eq('run_id', r.id)
      return {
        id: r.id,
        name: r.name,
        choices: Object.fromEntries(
          (decisions ?? []).map(d => [d.butterfly_effect_name, d.chosen_option])
        ),
      }
    })
  )

  return (
    <main className="min-h-screen px-4 py-8 max-w-lg mx-auto pb-24">
      <div className="flex items-center gap-3 mb-6">
        <Link href={`/runs/${id}`} className="text-horror-muted tap-target flex items-center">
          <ArrowLeft size={20} />
        </Link>
        <h1 className="font-cinzel text-xl font-bold text-horror-text">Run-Vergleich</h1>
      </div>

      <RunTimeline
        currentRun={{
          id: currentRun.id,
          name: currentRun.name,
          choices: Object.fromEntries(
            (currentDecisions ?? []).map(d => [d.butterfly_effect_name, d.chosen_option])
          ),
        }}
        otherRuns={otherRunsWithDecisions}
      />
    </main>
  )
}
```

- [ ] **Step 3: Build + Verify**

```bash
npm run build 2>&1 | tail -30
```

Expected: Successful build, no TypeScript errors.

- [ ] **Step 4: Commit**

```bash
git add src/components/comparison/RunTimeline.tsx src/app/runs/[id]/compare/page.tsx
git commit -m "feat: run timeline — chapter-by-chapter divergence view with expand-on-tap"
```

---

## Task 6: Lokalisierung — Deutsch

**Files:**
- Modify: `src/app/runs/page.tsx`
- Modify: `src/app/runs/new/page.tsx`
- Modify: `src/components/characters/CharacterCard.tsx`
- Modify: `src/app/runs/layout.tsx`

- [ ] **Step 1: runs/page.tsx — deutsche Texte**

In `src/app/runs/page.tsx` folgende Strings ersetzen:

```typescript
// "Your Runs" → "Deine Runs"
<h1 className="text-2xl font-bold text-horror-text font-cinzel">Deine Runs</h1>

// "+ New Run" button:
<Plus size={16} />
Neuer Run

// Leer-Zustand:
<p className="text-horror-muted">Noch keine Runs vorhanden.</p>
<p className="text-horror-muted text-sm mt-1">Erstelle deinen ersten Run, um zu beginnen.</p>
```

- [ ] **Step 2: runs/new/page.tsx — deutsche Texte**

In `src/app/runs/new/page.tsx` folgende Strings ersetzen:

```typescript
// Zurück-Link:
<ArrowLeft size={16} /> Zurück

// Titel:
<h1 className="text-2xl font-bold text-horror-text mb-6 font-cinzel">Run benennen</h1>

// Input-Placeholder:
placeholder="z.B. Alle überleben"

// Create-Button:
{loading ? 'Erstelle…' : 'Run erstellen'}

// Vorschlag-Text:
<p className="text-horror-muted text-sm mb-3">Oder Vorschlag wählen:</p>

// Suggested names:
const SUGGESTED_NAMES = [
  'Run 1 – Alle überleben',
  'Run 2 – Chaos-Run',
  'Perfekter Run',
  'Alle sterben',
]
```

- [ ] **Step 3: CharacterCard.tsx — deutsche Status-Labels**

In `src/components/characters/CharacterCard.tsx` die `STATUS_LABELS` anpassen:

```typescript
const STATUS_LABELS: Record<Status, string> = {
  alive:   'LEBT',
  dead:    'TOT',
  unknown: '?',
}
```

- [ ] **Step 4: Commit**

```bash
git add src/app/runs/ src/components/characters/CharacterCard.tsx
git commit -m "feat: vollständige deutsche Lokalisierung"
```

---

## Task 7: CLAUDE.md aktualisieren

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: CLAUDE.md vollständig aktualisieren**

Ersetze den Inhalt von `CLAUDE.md` mit der neuen Architektur, Komponenten, Design-Entscheidungen.

Key changes to document:
- Neue Architektur: CardNavigator + ButterflyCard statt DecisionList
- Gelöschte Komponenten: DecisionCard, DecisionList, TimedDecision, TimedModeButton, ComparisonView
- Neue Komponenten: ButterflyCard, CardNavigator, RunTimeline
- Design: Cinzel Decorative Font, Eisblau (#7db8cc) für Unknown-Status, Gold (#c9a84c) für Akzente
- butterfly-effects.ts: neue Felder `nameDe`, `descriptionDe`, `optionsDe`, `sceneImage`
- DB-Kompatibilität: `options` bleibt als DB-Key, `optionsDe` nur für Display
- Sprache: App vollständig auf Deutsch
- Kein Timed Mode mehr (entfällt)
- Run-Vergleich: direkt in jede Karte integriert (Badges) + eigene Timeline-Seite

- [ ] **Step 2: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: CLAUDE.md aktualisiert — neue Architektur, Karten-UX, Design-Tokens"
```

---

## Self-Review

**Spec-Coverage:**
- ✅ Task 1: Charakterstatus Unbekannt → Eisblau
- ✅ Task 2a: Cinzel Decorative Font
- ✅ Task 2b: Sanduhr mit "UNTIL DAWN" Schriftzug + Gold-Akzenten
- ✅ Task 2c: Bergsilhouette — scharfe Konturen, Schnee-Highlights, Nebel-Basis
- ✅ Task 2d: Blitz-Effekte — SVG-Blitze die wandern, unregelmäßig, dramatisch
- ✅ Task 3: Deutsche Übersetzungen in butterfly-effects.ts, DB-Kompatibilität erhalten
- ✅ Task 4: Karten-UX — eine Karte pro Szene, Swipe/Pfeil-Navigation, Szenenbilder, Run-Badges
- ✅ Task 4 (Undo): Erneuter Klick → Auswahl aufheben + Supabase-Delete
- ✅ Task 4 (Timed Mode): Entfernt, ersetzt durch Karten-UX
- ✅ Task 5: Run-Zeitstrahl mit Divergenz-Highlighting, expandierbar
- ✅ Task 6: Alle UI-Texte auf Deutsch
- ✅ Task 7: CLAUDE.md aktualisiert

**DB-Kompatibilität:** Bestehende `decisions`-Einträge bleiben gültig — `chosen_option` speichert weiterhin die englischen `options`-Werte. Die deutschen Labels (`optionsDe`) sind rein display-seitig.

**Placeholder-Scan:** Alle Code-Blöcke vollständig. Szenen-Screenshots werden während Task 4 recherchiert.

**Typ-Konsistenz:**
- `effect.options[i]` = DB-Key (immer englisch)
- `effect.optionsDe[i]` = Display-Label (immer deutsch)
- Beide Arrays sind parallel indiziert — `options[0]` entspricht `optionsDe[0]`
- `ButterflyCard` verwendet `options[i]` als `dbOption` und `optionsDe[i]` als `label`
